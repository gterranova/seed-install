import { Routes } from '@angular/router';
// app
import { Config } from '../common/index';

const basePath = Config.IS_WEB ? '' : './';

export const {{ properCase module-name }}Routes: Routes = [
];
